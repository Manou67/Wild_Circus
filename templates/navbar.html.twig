<nav class="navbar sticky-top navbar-expand-lg">
    <a class="nav-link bigtitle" href="{{ path('home') }}">Wild Circus</a>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
        <ul class="navbar-nav">
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    Spectacles</a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                    <a class="dropdown-item" href="{{ path('spectacle_list') }}">Nos spectacles</a>
                    <a class="dropdown-item" href="#">Anciens spectacles</a>
                </div>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('ticket_index') }}">Billets<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('news_list') }}">Actualités<span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('team_list') }}">L'équipe Wild Circus<span class="sr-only">(current)</span></a>
            </li>
            <div class="connect">
            {% if app.user %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <div id="admin_link">
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    Compte Admin</a>
                                <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                                    <a class="dropdown-item" href="{{ path('spectacle_index') }}">Gestion des spectacles</a>
                                    <a class="dropdown-item" href="{{ path('ticket_index') }}">Gestion des billets</a>
                                    <a class="dropdown-item" href="{{ path('user_index') }}">Gestion des utilisateurs</a>
                                    <a class="dropdown-item" href="{{ path('news_index') }}">Gestion de l'actualité de Wild Circus</a>
                                    <a class="dropdown-item" href="{{ path('team_index') }}">Gestion de l'équipe de Wild Circus</a>
                                </div>
                            </li>
                        </div>
                    {% endif %}
                <a id="connexion" class="nav-link" href="{{ path('profile') }}"><button class="btn btn-md btn-light">Mon profil</button><span class="sr-only">(current)</span></a>
                <a id="connexion" class="nav-link" href="{{ path('app_logout') }}"><button class="btn btn-md btn-light">Se déconnecter</button><span class="sr-only">(current)</span></a>
               {% else %}
                <a id="connexion" class="nav-link" href="{{ path('app_login') }}"><button class="btn btn-md btn-light">Connexion</button><span class="sr-only">(current)</span></a>
                <a id="connexion" class="nav-link" href="{{ path('app_register') }}"><button class="btn btn-md btn-light">Inscription</button><span class="sr-only">(current)</span></a>
            {% endif %}
            </div>
        </ul>
    </div>
</nav>
